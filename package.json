{
  "name": "discord-voice-join-logger",
  "version": "0.1.0",
  "private": true,
  "description": "Logs voice channel join events to a text channel.",
  "main": "src/index.js",
  "scripts": {
    "deploy": "node scripts/deploy.js",
    "deploy:commands": "node scripts/deploy-commands.js",
    "start": "node src/index.js",
    "migrate:postgres": "node scripts/migrate-sqlite-to-postgres.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "discord.js": "^14.0.0",
    "dotenv": "^16.4.5",
    "pg": "^8.13.1"
  },
  "devDependencies": {
    "better-sqlite3": "^11.6.0"
  }
}
