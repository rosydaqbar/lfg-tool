# LFG Tool Documentation

Nextra docs-theme site for this repository.

```bash
npm --prefix documentation install
npm --prefix documentation run dev
npm --prefix documentation run build
npm --prefix documentation run start
```

The docs content lives in `documentation/content`.
