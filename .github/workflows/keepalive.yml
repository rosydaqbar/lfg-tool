name: Keep Koyeb Alive

on:
  schedule:
    - cron: "*/20 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Koyeb app
        run: |
          curl -fsS --max-time 20 "https://inevitable-marj-ocid-349c510b.koyeb.app" > /dev/null
